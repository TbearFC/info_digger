ANTHROPIC_API_KEY=your_key_here
DB_PATH=./info_digger.db
GITHUB_TOKEN=           # optional — raises rate limit from 60 to 5000 req/hr
DB_BACKFILLED=          # set to "1" after first backfill completes (auto-set by app)
